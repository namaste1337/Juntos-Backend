#!/usr/bin/env nodemon
// Requires
var app 	 = require("./../app");

// Constants
var port     = process.env.PORT || 8080;

// Begin listening for incoming request
app.listen(8080, function(){
	console.log("This Magic happens on port 8080")
});
